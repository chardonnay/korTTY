# Versionshinweise

Was hat sich in der aktuellen Version geändert? Die Version, für die diese Anleitung erstellt wurde, wird in der Fußzeile angezeigt.

## v2.9.0

### Terminalprotokollierung

- **Verbindungsprotokollierung wurde neu erstellt, um tatsächlich zuverlässig zu sein** – [Terminalprotokollierung](../features/terminal.md#terminalprotokollierung) schrieb zuvor jede Verbindung auf einen festen Pfad (so dass zwei gleichzeitig geöffnete Verbindungen in dieselbe Datei verschachtelt waren), hatte keine Komprimierung und wurde durch *Löschen* der Datei bei der Größenbeschränkung rotiert. Dateien heißen jetzt `<date>-<time>-<server>_<number>`, jeden Tag wird eine neue Datei gestartet (optional) und wann immer die Größenbeschränkung erreicht wird, wird nichts durch Rotation überschrieben oder gelöscht, und jede geschlossene Datei wird gzip-komprimiert. Sobald die Aufbewahrungsfrist (Standard 30 Tage, `0` behält alles) abläuft, werden alte Archive nach dem in ihrem Namen kodierten Datum bereinigt und nur die Protokolldateien von korTTY selbst werden jemals berührt, daher ist es sicher, die Einstellung auch auf einen Ordner zu verweisen, der für andere Zwecke verwendet wird.
- **Das Terminalprotokoll ist jetzt geschwärzt** – erfasste Zeilen durchlaufen die gleiche Schwärzung wie das Sitzungsjournal, bevor etwas geschrieben wird: das eigene gespeicherte Passwort der Verbindung und alle Ersetzungsregeln, die in der Richtlinie einer Organisation definiert sind. Protokolldateien und ein Protokollordner, den korTTY selbst erstellt hat, sind auf Nur-Eigentümer-Berechtigungen eingestellt, sofern das Dateisystem dies unterstützt.
- **Quick Connect hat einen Terminal-Protokollabschnitt erhalten** – Beim Konfigurieren der Protokollierung im Verbindungsmanager und beim anschließenden Herstellen einer Verbindung über Quick Connect mit einer anderen Authentifizierungsmethode wurde die Einstellung zuvor stillschweigend gelöscht. Quick Connect verfügt jetzt über eigene Ordner-, Format- und Komprimierungssteuerelemente und wendet diese auf die geöffnete Sitzung an.
- **Behoben: XML- und JSON-Protokolle hatten zwei Dokumentstämme und wurden nie analysiert**, die Rotation schrieb die schließende Fußzeile zweimal, bis zu 10.000 Zeilen in der Warteschlange kurz vor einer Unterbrechung konnten stillschweigend gelöscht werden, und die Ausgabe wurde an den Zeilenenden so aufgeteilt, dass Einrückungen zerstört wurden und jeder Fortschrittsbalkenrahmen in einen eigenen Protokolleintrag umgewandelt wurde. Alle vier sind behoben.

### Sitzungsjournal: gefilterter Export, benannte Markierungen, Seitendarstellung und Screenshot-Bearbeitung

- **Exporte können jetzt eingegrenzt werden** – der [Exportdialog](../features/session-journal.md#es-wird-nur-ein-teil-eines-journals-exportiert) akzeptiert eine beliebige Anzahl von Zeitfenstern (ein Eintrag muss nur in eines davon fallen), einen Themenfilter als wörtlichen Text, einen regulären Ausdruck oder an die KI übergeben, um ihn nach Bedeutung auszuwählen, und einen Markierungsfilter (alle Einträge, nur markierte oder bestimmte Markierungen). Die Zeiten werden nachsichtig geparst (`8`, `0800`, `8.30` funktionieren alle) und um eine einstellbare Toleranz erweitert (standardmäßig ± 5 Minuten), sodass ein ungefähres Fenster immer noch den Eintrag erfasst, der tatsächlich darüber liegt. Ein gefiltertes HTML-Bundle wird neu erstellt und nicht kopiert – gekürztes Capture-Log, nur die referenzierten Screenshots –, sodass ein gemeinsamer Export den Rest der Sitzung nicht preisgeben kann; Jedes gefilterte PDF, jede Markdown-Datei und jedes Bundle trägt ein Banner, das den Bereich benennt, sodass niemand es mit dem gesamten Journal verwechselt.
- **Markierungen sind keine feste Liste mit vier Werten mehr** – über die integrierten Funktionen (Keine, Info, Wichtig, Fehler) hinaus können Sie jetzt eigene [benannte, farbige Markierungen](../features/session-journal.md#markers) definieren, die jeweils zu einer der integrierten Funktionen für ältere Journale und für die Fehler-/Wichtig-Zählungen herabgestuft werden. Optionale Regeln wenden beim Erfassen von Einträgen automatisch eine Markierung an, die einen wörtlichen Begriff oder einen regulären Ausdruck mit dem Titel, der Zusammenfassung, der Notiz und den Auszügen des Eintrags abgleicht, mit Vorrang **Ihre eigene Wahl > eine Regel > der Vorschlag der KI** – eine Regel überschreibt niemals eine von Ihnen manuell festgelegte Markierung.
- **Die Journalseite verfügt über ein anpassbares [Erscheinungsbild](../features/session-journal.md#aussehen)** – ein Farbschema (sechs feste Paletten, das automatische Hell/Dunkel-Paar der Seite oder ein live von den Farben Ihres Terminals abgeleitetes Schema), eine Textschrift und eine Festbreitenschrift, die aus einem Symbolleisten-Popover ausgewählt und für jedes anschließend von korTTY gerenderte Journal gespeichert wird.
- **Screenshots können [kommentiert werden](../features/session-journal.md#screenshot-notizen-und-anmerkungen)** – Stift, Box, ein „Unleserlich“-Werkzeug, das einen Bereich in Blöcke vergröbert, und Textbeschriftungen sowie ein Notizfeld, das durch Rechtsklick auf einen Screenshot erreichbar ist. Markierungen werden als Daten gespeichert und bleiben wiederbearbeitbar; Die unberührte Aufnahme wird lokal als `.orig.png` gespeichert und nie in einen Export einbezogen. Dadurch wird ein Wert auf der gerenderten Seite verdeckt, nicht geschwärzt – verwenden Sie „Suchen & ersetzen“ oder eine Richtlinien-Schwärzungsregel, um etwas endgültig aus einem Journal zu entfernen.
- **Das Auswählen eines Zeitbereichs ist einfacher geworden** – in korTTY kann die Zeitleiste des Viewers in den Bereichsauswahlmodus umgeschaltet werden: Klicken Sie auf einen ersten und einen letzten Eintrag, um die Zeitspanne dazwischen hervorzuheben und ihn direkt an den Exportdialog zu übergeben, oder sammeln Sie vor dem Exportieren mehrere Fenster. Die gleiche Auswahl ist in der Eintragstabelle im Bearbeitungsmodus per Rechtsklick verfügbar.
- **Der Titel des Journals kann direkt auf der Seite bearbeitet werden**, und eine Kopfzeilenschaltfläche springt zwischen markierten Einträgen, wenn das Journal welche hat.

!!! note "Frühere Versionen"
    Hier wird nur die aktuelle Version aufgeführt, daher bleibt der Anleitung in jeder Sprache, in die er übersetzt wurde, kurz. Jede Version ist auf der [GitHub-Release-Seite ](https://github.com/chardonnay/korTTY/releases); Die kuratierten Notizen für frühere Versionen werden im Repository in `app-docs/release-notes-archive.md` und `app-docs/RELEASE_NOTES.adoc` aufbewahrt.
